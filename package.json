{
    "name": "ts-utils-julseb",
    "version": "2.3.3",
    "private": false,
    "description": "Small utils components in TypeScript",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "clean": "rm -rf ./lib && mkdir lib && rm -rf ./src/*.js && rm -rf ./index.js",
        "move-to-lib": "mv ./src/*.js ./lib",
        "build-all": "npm run clean && tsc",
        "build": "babel src -d lib"
    },
    "author": "Julien Sebag",
    "license": "MIT",
    "devDependencies": {
        "@babel/cli": "^7.24.8",
        "@babel/core": "^7.24.8",
        "@types/node": "^20.14.10",
        "typescript": "^5.5.3"
    },
    "dependencies": {
        "@babel/plugin-syntax-import-assertions": "^7.24.7"
    }
}
