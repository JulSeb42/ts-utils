{
    "name": "@julseb-lib/utils",
    "version": "0.0.24",
    "private": false,
    "description": "Small utils components in TypeScript",
    "main": "index.js",
    "type": "module",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "clean": "rm -rf ./lib && rm -rf ./src/*.js && rm -rf ./index.js && rm -rf ./test.js",
        "build": "npm run clean && tsc",
        "prepublishOnly": "pnpm build",
        "test-fn": "pnpm tsx test.ts"
    },
    "author": "Julien Sebag",
    "license": "MIT",
    "devDependencies": {
        "@types/web": "^0.0.237",
        "tsx": "^4.20.5",
        "typescript": "^5.5.3"
    }
}
