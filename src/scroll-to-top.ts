/*=============================================== Scroll to top of the page ===============================================*/

export function scrollToTop(): void {
    return window.scrollTo(0, 0)
}
